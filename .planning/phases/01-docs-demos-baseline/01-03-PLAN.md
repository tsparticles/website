---
wave: 2
plan: 03-CI-Lint-and-Tests
depends_on: []
files_modified:
  - .github/workflows/nodejs.yml
  - package.json
  - .prettierrc
autonomous: false
---

# Plan 03: CI Linting and Basic Tests

Objective: Add linting and a minimal test suite for demo-related code so PRs are checked for regressions.

Tasks:

1. TASK-CI-01: Add linting step
   - Add ESLint config targeting `js/` and `docs-gen/` (recommend `eslint:recommended` + node/browser envs).
   - Add `lint` script to `package.json` and run it in CI on PRs.

2. TASK-CI-02: Add a minimal test runner
   - Introduce `vitest` or `jest` for unit tests. Add a simple test for `applyPartialConfig`/`applyFullConfig` behavior (mocked DOM).
   - Add `test` script and run in CI.

3. TASK-CI-03: Prettier pre-commit
   - Ensure Prettier is enforced via pre-commit or CI check (existing `.prettierrc` present).

Must haves:

- CI runs lint and tests and blocks merges on failure.

Verification:

- CI passes for a test PR (or local run of `pnpm test` + `pnpm lint`).

Estimated effort: 1-3 days.
